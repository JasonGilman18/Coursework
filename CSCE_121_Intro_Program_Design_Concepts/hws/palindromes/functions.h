#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <string>

void printUsageInfo(std::string);
bool isPalindrome(std::string, bool, bool);
bool isPalindromeR(std::string);
std::string preprocessString(std::string, bool);

#endif